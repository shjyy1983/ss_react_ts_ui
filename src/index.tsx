// export { default as SButton} from '@components/SButton';
// export { default as SIcon} from '@components/SIcon';

import SButton from '@components/SButton';
import SIcon from '@components/SIcon';

export default {
  SButton,
  SIcon
}